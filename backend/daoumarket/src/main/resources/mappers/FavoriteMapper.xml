<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="favorite">
	<insert id="insertFavorite" parameterType="favorite">
		insert into favorite(user_id, item_id) values(#{userId}, #{itemId})
	</insert>
	
	<delete id="deleteFavorite">
		delete from favorite where favorite_id=#{favoriteId}
	</delete>
	
	<select id="getFavoriteList" parameterType="int" resultType="itemResponse">
		select i.item_id, i.user_id, i.title, i.price, i.content, i.category, i.status, i.delflag, i.date
		from item i, favorite f
		where f.user_id = #{userId} and i.item_id = f.favorite_id and delflag = 0
		order by date desc
	</select>
	
	<select id="isFavorited" parameterType="itemInfoRequest" resultType="boolean">
	  select if(count(*) = 1, 1, 0)
	  from favorite
	  where item_id = #{itemId} and user_id = #{userId}
	</select>
</mapper>